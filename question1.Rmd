---
title: "question1"
output: html_document
---

```{r question1, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
```

```{r}
# want to figure out how to order the column chart by n...
by_state <- merged %>% group_by(State) %>% tally() %>% arrange(n)
by_state %>% ggplot(aes(x=State,y=n)) + geom_col() + ggtitle('Breweries per State') + xlab('State') + ylab('Count')
```

